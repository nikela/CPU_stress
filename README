Description: CPU-intensive benchmark, running Intel MKL GEMM with double-precision. The matrix sizes autotune to the L1 cache size.

To compile, type:
	make

To run:
	./gemm <time_to_completion> <num_threads>


